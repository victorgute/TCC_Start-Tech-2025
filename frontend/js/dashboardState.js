/**
 * Este ficheiro exporta um objeto de estado partilhado.
 * Ele permite que diferentes scripts (como ferramentas.js e chatbot.js)
 * comuniquem e partilhem dados em tempo real.
 */
export const dashboardState = {
    metrics: null, // Aqui guardaremos os dados calculados dos dashboards
};