/**
 * Este ficheiro exporta um objeto vazio `charts`.
 * Ele serve como um "armazém" central para as instâncias dos nossos gráficos.
 * Ao importá-lo em diferentes ficheiros, garantimos que todos estão a
 * aceder e a modificar as mesmas instâncias de gráficos, evitando conflitos.
 */
export let charts = {};

